# Plan: DSO Bug Fixes & Enhancements
Date: 2026-06-09

## Issues Found in Review (RIPER Phase 1)
1. 🔴 Critical: `runningWindowMs` slider is dead code — display width hardcoded
2. 🟡 Medium: Dynamic JSON `import()` fragile in production
3. 🟡 Medium: `displayXs` has unused `xs` dependency — extra re-renders
4. 🟡 Medium: `calibrate()` reads delayed `displayValues` when running
5. 🟢 Minor: `windowSize` prop in Waveform is dead code
6. 🟢 Minor: Misleading comment says "fixed X axis 0→windowMs"

## User's Choices (RIPER Phase 2)

### Issue 1: Slider (Choice C)
Slider controls **number of captures visible** (1–10) instead of time ms.
- Rename state: `runningWindowMs` → `capturesVisible`
- Change slider: min=1, max=10, step=1
- `displaySize = effectiveNumSamples * capturesVisible`
- Label: "Captures visible: N"

### Issue 2: JSON Import (Choice A)
Static import at top of file.
- Move `import noiseBaseline from "../pokit/noiseBaseline.json"` to top
- Remove dynamic `import()` and `.default` access
- Use `noiseBaseline` directly in useEffect

### Issue 3: Unused Dependency (Choice A)
Remove `xs` from `displayXs` dependency array.
- Only deps: `[running, displaySize, displayValues.length, sampleRate]`

### Issue 4: Calibrate (Choice ABC)
Add dropdown/button group for calibration source:
- "Calibrate" button opens small menu with 3 options:
  - **Current view** (uses `displayValues` — what's on screen)
  - **Full buffer** (uses `values` — all accumulated data)
  - **Reset to repo** (reloads `noiseBaseline.json`)
- Also store calibrated baselines in `localStorage` keyed by MAC address
- On mount: try `localStorage` → fallback to repo JSON

### Issue 5: windowSize Prop (Choice B)
Keep `windowSize` prop and related logic in Waveform.tsx.
- Might be useful for future one-shot windowing features
- No changes needed

### Issue 6: Comment (Choice A)
Fix comment on line 74 to match actual contiguous-time behavior.

## Implementation Order
1. Issue 2: Static import + Issue 3: Remove dep (quick wins, no UI change)
2. Issue 1: Wire captures-visible slider (changes UI, test)
3. Issue 4: Calibration source menu + localStorage by MAC
4. Issue 6: Fix comment
5. Commit all changes
